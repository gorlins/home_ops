machine:
  kubelet:
    extraMounts:
      - destination: task
        type: bind
        source: /var/mnt/data/
        options:
          - bind
          - rshared
          - rw
      - destination: /var/mnt/longhorn
        type: bind
        source: /var/mnt/longhorn
        options:
          - bind
          - rshared
          - rw
