machine:
  kubelet:
    extraMounts:
      - destination: task
        type: bind
        source: /var/mnt/data/
        options:
          - bind
          - rshared
          - rw
      # Rook data directory
      - destination: /var/lib/rook
        type: bind
        source: /var/lib/rook
        options:
          - bind
          - rshared
#      - destination: /var/mnt/longhorn
#        type: bind
#        source: /var/mnt/longhorn
#        options:
#          - bind
#          - rshared
#          - rw
