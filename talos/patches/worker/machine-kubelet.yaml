machine:
  kubelet:
    extraMounts:
      - destination: /var/mnt/data/longhorn
        type: bind
        source: /var/mnt/data/longhorn
        options:
          - bind
          - rshared
          - rw
