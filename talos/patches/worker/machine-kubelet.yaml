machine:
  kubelet:
    extraMounts:
      - destination: /var/lib/longhorn/
        type: bind
        source: /var/lib/longhorn/
        options:
          - bind
          - rshared
          - rw
      - destination: task
        type: bind
        source: /var/mnt/data/
        options:
          - bind
          - rshared
          - rw
