machine:
  kubelet:
    extraMounts:
      - destination: /var/lib/longhorn
        type: bind
        source: /var/mnt/local-path-storage/longhorn
        options:
          - bind
          - rshared
          - rw
