machine:
  files:
    - path: /etc/cri/conf.d/20-customization.part  # Required for spegel to work https://spegel.dev/docs/getting-started/#talos
      op: create
      content: |
        [plugins."io.containerd.cri.v1.images"]
          discard_unpacked_layers = false
