apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: backblaze-store
spec:
  configuration:
    destinationPath: "s3://scottgorlin-k8s-backup/cnpg"
    endpointURL: "https://s3.us-west-000.backblazeb2.com"
    s3Credentials:
      accessKeyId:
        name: backblaze-credentials
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: backblaze-credentials
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
