services:
  tunnel:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    network_mode: host
    command: tunnel run
    volumes:
      - /etc/localtime:/etc/localtime:ro
      # - .:/etc/hosts
    environment:
      TUNNEL_TOKEN:
